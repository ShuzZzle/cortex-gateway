import { withClasses } from "../../util";
import React from "react";
import styles from "./toolbar.module.css";

const Wrapper = withClasses("div", styles.wrapper);

export function Toolbar() {
  return (
    <Wrapper>
    </Wrapper>
  );
}
